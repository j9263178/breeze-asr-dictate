# 一般套件(pip install -r requirements.txt)
transformers
sounddevice
numpy
keyboard
pyperclip
requests
python-dotenv
edge-tts
accelerate
safetensors

# PyTorch 需另外指定 CUDA 版本安裝(依你的 CUDA 版本選):
#   CUDA 12.6(RTX 40 系列):
#     pip install torch --index-url https://download.pytorch.org/whl/cu126
#   CUDA 12.1:
#     pip install torch --index-url https://download.pytorch.org/whl/cu121
